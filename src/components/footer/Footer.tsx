import styles from "./footer.module.css";

export const Footer = () => {
  return (
    <footer className={`${styles.footer} container section`}>
      <h3 className={styles.footer__title}>Denis Scherbakov</h3>
    </footer>
  );
};
